package tutu.titi.toto;

public class A {
    int test() {
        4 = 3;
        43.0 = (float)43;
        // 'c' = true;
        // "Test" -= null;
        // "float" += 4.6;
        4++;
        --27.;

        int c = (3 + 3);
        if (true) {
          (true? c: 2);
        }

        // "void.class" = void.class;
        // "int.class" = int.class;
        // "intarray.class" = int[].class;
        // "Instanceof" = 23 instanceof Double;

        throw 45=3;

        if (43==3) {
            43++;
        } else {
            43--;
        }

        while(true) {
            43++;
        }

        return 34;
    }
}
